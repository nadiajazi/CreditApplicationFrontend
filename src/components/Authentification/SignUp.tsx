import React from 'react'

function SignUp() {
  return (
    <div>
      {/* UI Elements:
        Sign-up form with fields for required information(Name, LastName, montantmaximal, NumCIN, Email, PassWord).
        Confirmation message about email verification.
      Functionality:
        Sends an email to the admin for approval.
        Sends an email to the client after admin approval.
        Redirects the user to the Email Verification Page.
    */}
    </div>
  )
}

export default SignUp
